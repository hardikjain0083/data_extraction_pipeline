# data_extraction_pipeline
## NER + Groq Integration

The `ner_groq.py` module performs named entity recognition on extracted text
and uses Groq LLM to structure tender information into JSON format.

### Environment Setup
Set the Groq API key using environment variables before running.
